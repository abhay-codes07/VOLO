# Normalize line endings to LF in the repository for all text files.
* text=auto eol=lf

# Binary / generated lockfiles — let Git detect, never normalize.
*.png binary
*.jpg binary
*.gif binary
*.ico binary
*.woff binary
*.woff2 binary
