# @visx/* still declares a React <=18 peer range, but works fine on React 19.
# legacy-peer-deps makes `npm install` resolve the tree instead of erroring with
# ERESOLVE — applied identically to local installs and the Docker build.
legacy-peer-deps=true
